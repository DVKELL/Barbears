//Renderiza un form para que el usuario seleccione si es cliente, barbero o Administrador

export const identifyUser = (_, res) => {
    res.send("Renderizar pagina para seleccionar si eres Cliente o Barbero");
}